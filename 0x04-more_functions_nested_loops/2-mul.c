#include "main.h"
/**
 * mul - multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: product of two integers
 */

int mul(int a, int b)
{
	int product = (a * b);

	return (product);
}
